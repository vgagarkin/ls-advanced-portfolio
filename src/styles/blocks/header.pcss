.header {
    padding: 64px 0;
    position: absolute;
    width: 100%;
    z-index: 1;

    .wrapper {
        display: flex;
        flex-direction: row;
        justify-content: space-between;
    }
}